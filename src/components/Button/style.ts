import { styled } from '@/stitches.config';
import { Hexile } from '../flexile';

export const Wrapper = styled(Hexile, {
  borderRadius: '10rem',
  outline: 'none',
  cursor: 'pointer',
  background: '$GRAY0',
  fontSize: '4rem',
  fontWeight: 800,
});
